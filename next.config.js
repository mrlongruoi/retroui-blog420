/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
        ignoreDuringBuilds: true,
    },
    javascript: {
        ignoreDuringBuilds: true
    }
}
module.exports = nextConfig
